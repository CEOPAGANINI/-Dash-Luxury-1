import FormularioLogin from "./FormularioLogin";

export const metadata = { title: "Entrar — Infinity" };

export default function PaginaLogin() {
  return (
    <main className="tela-login">
      <FormularioLogin />
    </main>
  );
}
